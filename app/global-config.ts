// Global configuration to force dynamic rendering for all pages
export const dynamic = 'force-dynamic';
export const revalidate = 0;

// Disable static generation for the entire app
export const runtime = 'nodejs';
